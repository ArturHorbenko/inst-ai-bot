# Copy to .env and fill in. The script reads .env at startup; values are only
# applied if the matching environment variable isn't already set.

# URL of the inst-ai-bot server this skill should call.
# Default (used when this file is absent): http://localhost:8000
INST_AI_BOT_URL=http://localhost:8000
